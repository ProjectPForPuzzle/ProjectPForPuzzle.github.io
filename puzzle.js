function translator(x, y) {
    this.x = x;
    this.y = y;
    this.width = 80;
    this.height = 80;
}

function key(x, y) {
    this.x = x;
    this.y = y;
    this.width = 80;
    this.height = 80;
}